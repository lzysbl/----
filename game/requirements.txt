pygame>=2.5.0
# 艾诺迪亚风格RPG游戏依赖包

# 核心游戏库
pygame>=2.5.0          # 游戏开发框架

# 可选的性能优化包
# numpy>=1.20.0        # 数值计算（可选，用于复杂数学运算）
# pillow>=8.0.0        # 图像处理（可选，用于图像加载和处理）

# 开发工具（可选）
# pygame-menu>=4.0.0   # 游戏菜单库（可选，用于更复杂的菜单系统）

# 注意：
# 1. 本游戏主要依赖pygame，其他包为可选
# 2. Python版本要求：3.7+
# 3. 如果在某些系统上pygame安装困难，可以尝试：
#    pip install pygame --user
#    或者
#    conda install pygame
